<template>
    <div class="{{GUI_CLASS}}">
        <!-- GUI Content -->
    </div>
</template>

<script lang="ts">
export default {
    name: '{{GUI_NAME}}',
    inject: ['rpgEngine', 'rpgKeypress', 'rpgGuiClose', 'rpgGui', 'rpgStage'],
    props: [],
    mounted() {
        // Initialize GUI
        this.rpgEngine.controls.stopInputs()
    },
    methods: {
        close() {
            this.rpgGuiClose('{{GUI_NAME}}')
            this.rpgEngine.controls.listenInputs()
        }
    }
}
</script>

<style scoped>
.{{GUI_CLASS}} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 100;
}
</style>
